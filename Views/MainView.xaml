<Window x:Class="ToolCollisionCalibration.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ToolCollisionCalibration.Views"
        mc:Ignorable="d"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Height="650" Width="1000" Loaded="Window_Loaded">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ToggleButton.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
        <!--<Style x:Key="TitleButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="微软雅黑"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" BorderThickness="0" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1C86EE"/>
                                <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="6,0,0,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->
    </Window.Resources>
    <materialDesign:DrawerHost x:Name="drawerHost" IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton,Path=IsChecked}">
        <materialDesign:DrawerHost.LeftDrawerContent>
            <StackPanel MinWidth="150" Orientation="Vertical" Background="#63B8FF">
                <Image Source="/ImageResources/飞机.png" Width="33" Height="33"/>
                <Button Style="{StaticResource TitleButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="ViewA" Height="Auto">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/ImageResources/主页.png" Width="33" Height="33"/>
                        <TextBlock Text="主页" Margin="10,0"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource TitleButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="ViewB" Height="Auto" Margin="0,30">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/ImageResources/设置.png" Width="33" Height="33"/>
                        <TextBlock Text="设置" Margin="10,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource TitleButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="ViewC" Height="Auto">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/ImageResources/操作.png" Width="33" Height="33"/>
                        <TextBlock Text="调试" Margin="10,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </materialDesign:DrawerHost.LeftDrawerContent>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <DockPanel x:Name="TitleDp" Background="DodgerBlue" LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <ToggleButton x:Name="MenuToggleButton" IsChecked="False" AutomationProperties.Name="HamburgerToggleButton" Style="{StaticResource MaterialDesignHamburgerToggleButton}"/>
                    <TextBlock Text="⌚" FontSize="25" VerticalAlignment="Center" Margin="10,0,0,0" Width="30"/>
                    <TextBlock Text="{Binding SystemTime}" FontSize="25" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock FontSize="25" VerticalAlignment="Center" Margin="0,0,20,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding localparams.DebugEnable}" Value="True">
                                        <Setter Property="Text" Value="正常模式中"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding localparams.DebugEnable}" Value="False">
                                        <Setter Property="Text" Value="调试模式中"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <!--<Image Width="30" Height="30" Margin="0,0,50,0">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Source" Value="/ImageResources/无信号.png"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding sqlClient.IsConnected}" Value="True">
                                        <Setter Property="Source" Value="/ImageResources/信号.png"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding sqlClient.IsConnected}" Value="False">
                                        <Setter Property="Source" Value="/ImageResources/无信号.png"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>-->
                    <Button x:Name="MinWinBtn" Content="—" VerticalAlignment="Center" FontSize="18"  Background="DodgerBlue" BorderThickness="0"/>
                    <Button x:Name="MinMaxWinBtn" Content="☐" VerticalAlignment="Center" FontSize="20"  Background="DodgerBlue" BorderThickness="0" Margin="20,0"/>
                    <Button x:Name="CloseWinBtn" Content="✕" VerticalAlignment="Center" FontSize="20"  Background="DodgerBlue" BorderThickness="0" Margin="0,0,5,0"/>
                </StackPanel>
            </DockPanel>
            <TextBlock Text="ToolCollisionCalibration" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" FontWeight="Bold"/>
            <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ViewContent"/>
        </Grid>
    </materialDesign:DrawerHost>
</Window>
