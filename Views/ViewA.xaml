<UserControl x:Class="ToolCollisionCalibration.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:chart="clr-namespace:ToolCollisionCalibration.Normal"
             xmlns:local="clr-namespace:ToolCollisionCalibration.Views"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1000">
    <Grid>
        <Border Background="Aquamarine">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>
                <Label Content="角度与扭矩笛卡尔坐标" HorizontalAlignment="Center"/>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="500"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <chart:ChartCanvas
                           Points="{Binding Points, Mode=OneWay}"
                           XMin="{Binding XMin, Mode=OneWay}"
                           XMax="{Binding XMax, Mode=OneWay}"
                           YMin="{Binding YMin, Mode=OneWay}"
                           YMax="{Binding YMax, Mode=OneWay}"
                           XAxisTitle="扭矩"
                           YAxisTitle="角度"
                           Background="White"
                           Margin="10,6,10,6"/>
                    <StackPanel Orientation="Vertical" Grid.Column="1">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="机二维码" FontSize="16"/>
                            <TextBox Text="{Binding dataBaseModel.SN_Code}" Width="170" FontSize="15" IsReadOnly="True" TextAlignment="Center" Margin="5,0,10,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="产品型号" FontSize="16"/>
                            <TextBox Text="{Binding dataBaseModel.ProductModel}" Width="170" FontSize="16" IsReadOnly="True" TextAlignment="Center" Margin="5,0,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="当前角度" FontSize="16"/>
                            <TextBox Text="{Binding deviceValueModel.RealAngle}" Width="170" FontSize="16" IsReadOnly="True" TextAlignment="Center" Margin="5,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
                <DataGrid Grid.Row="3" x:Name="LogDataGrid" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False" CanUserSortColumns="False" FontSize="15">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding Time}"/>
                        <DataGridTextColumn Header="类型">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="{x:Type DataGridCell}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type DataGridCell}">
                                                <Border x:Name="Boder">
                                                    <TextBlock Text="{Binding LogType}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <DataTrigger Binding="{Binding LogType}" Value="消息">
                                                        <Setter TargetName="Boder" Property="Background" Value="Green"/>
                                                    </DataTrigger>
                                                    <DataTrigger  Binding="{Binding LogType}" Value="提示">
                                                        <Setter TargetName="Boder" Property="Background" Value="Yellow"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding LogType}" Value="错误">
                                                        <Setter TargetName="Boder" Property="Background" Value="Red"/>
                                                    </DataTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="信息" Binding="{Binding Message}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>
    </Grid>
    
</UserControl>
